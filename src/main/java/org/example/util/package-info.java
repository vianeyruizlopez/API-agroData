/**
 * Utilidades del sistema AgroData.
 * <p>
 * Este paquete incluye la clase que ayuda a resolver tareas comunes dentro del sistema,
 * como generar tokens, validar datos, convertir formatos o manejar funciones repetitivas.
 * <p>
 * Las utilidades no dependen de la lógica de negocio ni de la base de datos, por lo que
 * pueden usarse desde cualquier parte del proyecto sin generar conflictos.
 * <p>
 * Su objetivo es mantener el código limpio, evitar duplicaciones y facilitar el mantenimiento.
 *
 * <p>Ejemplo incluido:
 * <ul>
 *   <li>{@code JwtUtil} – generación y validación de tokens JWT.</li>
 * </ul>
 */
package org.example.util;