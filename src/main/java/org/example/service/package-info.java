/**
 * Servicios del sistema AgroData.
 * <p>
 * Este paquete contiene las clases que se encargan de la lógica principal del sistema.
 * Aquí se decide qué hacer con los datos que llegan desde los controladores, cómo
 * procesarlos, validarlos y enviarlos a los repositorios para guardarlos.
 * <p>
 * Los servicios ayudan a mantener el código organizado, separando lo que hace el sistema
 * de cómo se muestra o cómo se guarda. También permiten reutilizar funciones en distintas
 * partes del proyecto.
 *
 * <p>Ejemplos incluidos:
 * <ul>
 *   <li>Interfaces como {@code ICatalogoCultivoService}, {@code IUsuarioService}.</li>
 *   <li>Implementaciones como {@code CatalogoCultivoService}, {@code UsuarioService}.</li>
 * </ul>
 */
package org.example.service;