/**
 * Rutas del sistema AgroData.
 * <p>
 * Este paquete organiza las rutas que conectan los controladores con los distintos
 * módulos del sistema. Cada clase define cómo se agrupan y exponen los endpoints
 * relacionados con cultivos, usuarios, tareas, talleres, asesorías, entre otros.
 * <p>
 * Las rutas ayudan a mantener el proyecto ordenado, separando la lógica de negocio
 * de la estructura de navegación de la API. También facilitan que otros sistemas
 * o aplicaciones se comuniquen con AgroData de forma clara y consistente.
 *
 * <p>Rutas incluidas:
 * <ul>
 *   <li>{@code RoutesCatalogoCultivo}</li>
 *   <li>{@code RoutesEstado}</li>
 *   <li>{@code RoutesNotificacion}</li>
 *   <li>{@code RoutesProyectos}</li>
 *   <li>{@code RoutesRegistroActividad}</li>
 *   <li>{@code RoutesReporteDesempeno}</li>
 *   <li>{@code RoutesSolicitudAsesoria}</li>
 *   <li>{@code RoutesSolicitudTaller}</li>
 *   <li>{@code RoutesTaller}</li>
 *   <li>{@code RoutesTarea}</li>
 *   <li>{@code RoutesTipoTerreno}</li>
 *   <li>{@code RoutesUsuario}</li>
 * </ul>
 */
package org.example.routers;